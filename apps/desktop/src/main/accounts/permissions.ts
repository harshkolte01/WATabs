/**
 * @deprecated Prefer session-permissions — kept as a thin re-export for callers.
 */
export { attachSessionPermissionHandlers as attachPermissionHandlers } from "../permissions/session-permissions";
